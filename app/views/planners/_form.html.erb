<%= form_with(model: planner) do |form| %>
  <% if planner.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(planner.errors.count, "error") %> prohibited this planner from being saved:</h2>

      <ul>
        <% planner.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
